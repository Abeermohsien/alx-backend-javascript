/**
 * returning  message on the STDOUT
 * @param {String} The message
 * @author Bezaleel Olakunori <https://github.com/B3zaleel>
 */
const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;
